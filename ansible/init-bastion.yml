- import_playbook: tasks/register-instance.yml
  vars:
    tagName: "DW Bastion"
    groupNames:
      - bastion

- name: Initialize bastion server
  hosts: bastion
  remote_user: ubuntu
  become: yes
  roles:
    - bastion
